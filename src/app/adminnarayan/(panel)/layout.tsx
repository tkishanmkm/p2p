
'use client';
// This layout has been moved to src/app/adminnarayan/layout.tsx to resolve parallel route conflicts.
// This file is no longer used but kept as a placeholder to avoid breaking the build if the directory still exists.
export default function ConflictResolutionLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
